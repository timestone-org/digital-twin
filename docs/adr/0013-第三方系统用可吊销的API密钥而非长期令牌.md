---
status: accepted
date: 2026-08-14
---

# ADR-0013：无人值守调用使用可吊销的账号 API 密钥

第三方系统使用归属于账号的 API 密钥，不延长不可即时吊销的 access JWT。密钥只存慢哈希、明文仅签发一次；每次认证检查吊销、到期和账号状态，再收敛成与 JWT 相同的调用者身份，因此下游不需要第二套权限体系。

密钥权限完全继承账号、不能进入 auth 管理面、只吊销不删除；需要更小权限时创建专用账号。嵌入页面如何把此密钥换成短期会话见 ADR-0051；模型部署自己的密钥是另一种业务凭据，见 ADR-0047。
