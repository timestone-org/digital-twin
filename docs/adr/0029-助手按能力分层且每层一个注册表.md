---
status: deprecated
date: 2026-08-31
---

# ADR-0029：助手按能力分层并使用显式注册表

“助手固定七层且每层都有 `ports.py + registry.py`”已被 `llmcore` 抽取后的实际边界取代，不再作为目录规范。仍成立的原则由各上下文维护：能力分层不等于执行流水线，扩展实现使用 Protocol 与显式注册而非 import 副作用；共享对话机制见 ADR-0032。
