---
status: accepted
date: 2026-08-12
---

# ADR-0009：空调原始数据由平台直读外部 EMS 库

`platform-server` 通过只读连接按需查询厂商 EMS，不复制、不缓存、不迁移其数据。实测查询成本不足以抵偿一条同步管线所需的水位、补洞、乱序、重试和保留期复杂度，且直读不会把旧数据伪装成实时数据。

EMS 不进入 readiness；不可用时仅相关数据面明确返回 503。对象名必须经过白名单、存在性和列形状校验，时序查询使用游标。需要跨空调聚合、保留被厂商删除的历史或实测压力不可接受时重新评估。
