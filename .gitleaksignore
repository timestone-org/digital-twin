# 机密扫描的误报豁免，一行一个指纹（`提交:路径:规则:起始行`）。
#
# ⚠ 这里只放**误报**。真写进过版本库的密钥不许用豁免糊过去——它在历史层里
# 删不掉，唯一的处置是重写历史并轮换那把钥匙。
#
# ⚠ 指纹钉在提交上：那条分支一旦变基，这行就自动失效、红灯重新亮起。那是对的，
# 新历史要重新判一遍，而不是继承一张旧的通行证。

# `test_crud_credentials.py:197` 守的正是「私钥绝不进库」这条约束：用例把一段
# 私钥 PEM 塞进公钥列，断言库里的 CHECK 把它挡下来。字面量的密钥体是 `MIIE`
# 四个字符，不是任何真钥匙；而 `private-key` 规则按 BEGIN 头匹配，必然命中。
223b0942ae5b306fb95c0284363f2bcf1fa969ee:server/services/opcua-server/tests/integration/test_crud_credentials.py:private-key:197

# 同一条用例随 opcua 分支 squash 合入 main（#15），提交换了 SHA，按新历史重判：
# 仍是「私钥绝不进库」用例里的四字符假 PEM，不是真钥匙。
1f10c7293135e098b5a38b241704de03ba6ff0a7:server/services/opcua-server/tests/integration/test_crud_credentials.py:private-key:197
