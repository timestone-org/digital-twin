"""模型账号的凭据表。**这一期只留表，不再读写**（ADR-0041）。

登录态已经归 platform 持有（与那一路供应商同属主），本服务改成经内部面领令牌。
表按扩展—收缩两次发布留着：代码可回滚、数据库不回滚，故「新结构 + 旧代码」
必须可用。删表在下一次发布。

⚠ 存量令牌**不搬**：迁移里禁止回填，跨服务读别人的密文也不该做。升级之后要在
模型管理页重新登录一次。
"""
